# CONTRACT

## 五项契约（改任何一项 = 大改版，必须更新本文）

1. **交付物**：原生 .pptx 为主产物；HTML 单文件仅作预览验稿与演讲者模式载体。
2. **用户**：自用优先；分享即分发 → 所有资产（源码/模板/字体/图标）许可必须干净，禁止合并 AGPL 代码。
3. **场景**：重要汇报为默认路线（导演式全流程）；快速出稿为轻量分支。
4. **主路线**：所有工作流必须输出符合 `schemas/slide.schema.json`（v 版本化）的中间表示；生成层只做渲染，禁止工作流与生成层耦合。
5. **质量门禁**：M0 起运行 P0 校验器（结构/溢出/乱码）；golden samples 做自动回归（截图 diff）；人工评分只在入库/换风格时执行。

## 测试集管理

- 初始集：v1（5~10 个典型输入：汇报/方案/培训/BP/提案等）
- 版本化规则：测试集增补 = 升版本（`test-sets/v1/` → `v2/`）；每轮验证记录必须绑定测试集版本号
- 基线判定：分数变化时先看测试集版本，再判断"我变差了"还是"测试变严了"

## 失败模式台账约定

- checklist 每条检查项编 `C-XXX` 编号
- 验证记录对号入座：失败模式 → 检查项编号 → 回归用例，形成闭环
