# 任务卡 T-M2B-R1 · v2 对照复核修订（P1 · 预计 1-2 小时）

指挥官：opencode ｜ 执行者：同一执行者 ｜ 版本：v1.0 ｜ 前置：T-M2B 已交付
> 依据：指挥官目视对照（`docs/visual-review-02.md`），发现 3 项问题 + 2 项建议落地。

## R1 · 内容完整性（最高优先）

**问题**：v1 P09 = "平台由四个模块组成"（layered-arch 三层架构）；v2 P09 = 章节页"03 方案与能力"，
且 v2 全 21 页未发现原架构内容 → 疑似内容丢失。

**要求**：
1. 澄清原 v1 P09 内容在 v2 中的去向（若并入其他页，给页码；若丢失，恢复）
2. 交付 **v2 页级映射表**（每页：v1 内容主题 → v2 版式 → 内容是否完整保留），写入报告
3. 恢复后总页数 ≤22；超限时允许替换章节页之一或合并，但**原 21 页的内容项必须全部可追踪**

## R2 · 章节体系一致性（二选一）

v2 引入了 03/05 两个章节页，但 01/02/04 无 → 半体系不自洽。
- 方案①：补齐 01/02/04 章节页（页数压力大）
- **方案②（推荐）**：对照版撤掉章节页体系（页码守恒），章节页族留给未来的正式多章 deck（≥3 章时启用完整体系）
说明所选方案与理由。

## R3 · CONTRACT v2 补充

1. **深色页预算**：≤15% 页数（锚点语义，防扩散）
2. **"骨架自检 ≠ 填充安全"**（C-011 教训）正式写入：骨架占位文案必须是该槽位允许的最长内容
3. 数据→几何的坐标计算注意（手工公式的易错点；自动化小工具列 backlog）

## R4 · C-010：subtier 第四类 `value`（附硬止损）

- `classify_subtier` 增加 `value`（纯数字+单位：`40 万`、`15 秒`、`1.8 年`）从 `short` 分离
- **硬止损（写死）**：value 引入后再出现**任何** free-design 误报 → **无条件**切换「页面主标题通道」，
  不再进行特征扩展（特征方案累计给三次机会：FIX2 修复 → FIX3 subtier → 本次 value）
- 靶场：t03-v2 的 3 项误报 → 0；全量回归；真阳性矩阵加 `value` 档样本

## R5 · W-6 更新

`init --dir` 为可靠写法（本次两次踩坑实测），更新 `docs/windows-notes.md` W-6。

## 验收标准

- [ ] v2 页级映射表（内容完整性可追踪，21 项全在）
- [ ] 章节体系方案落地（②或①）+ R1 恢复后全链路重跑
- [ ] checker 全过、D-2 P0=0、占位符 0；新 contact sheet 可读
- [ ] C-010 靶场 3→0 + value 档真阳性样本
- [ ] CONTRACT v2 三条补充；W-6 更新
- [ ] vendor 零 diff；提交（`fix(m2): v2 completeness restore + subtier value class (T-M2B-R1)`）
